{
  "name": "@repo/fn-bulk-products",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsx ../../scripts/lambda/build.ts --name fn-bulk-products",
    "clean": "rm -rf .lambda",
    "local": "SERVICE_NAME=fn-bulk-products DEBUG=1 IS_LOCAL=true tsx src/index.ts",
    "lambda:upload": "../../scripts/lambda/upload.sh",
    "lambda:deploy": "../../scripts/lambda/deploy.sh",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.878.0",
    "@aws-sdk/client-sqs": "^3.637.0",
    "@aws-sdk/lib-storage": "^3.878.0",
    "@repo/event-contracts": "workspace:*",
    "@repo/logger": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "eslint": "^8.57.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
