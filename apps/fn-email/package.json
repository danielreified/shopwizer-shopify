{
  "name": "@repo/fn-email",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsx ../../scripts/lambda/build.ts --name fn-email",
    "clean": "rm -rf .lambda",
    "local": "DEBUG=1 IS_LOCAL=true tsx src/index.ts",
    "send:email": "tsx ./scripts/send-email-job.ts",
    "lambda:upload": "../../scripts/lambda/upload.sh",
    "lambda:deploy": "../../scripts/lambda/deploy.sh",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "@prisma/client": "6.18.0",
    "@repo/react-email": "workspace:*",
    "resend": "^6.4.1",
    "@repo/logger": "workspace:*"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@types/aws-lambda": "^8.10.124",
    "esbuild": "^0.25.9",
    "eslint": "^8.57.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
